"use client"; import { use } from "react"; import { useDocument } from "@/hooks/use-documents"; import { DocumentForm } from "@/components/features/documents/DocumentForm"; import { LoadingSpinner } from "@/components/common/LoadingSpinner"; import { ErrorMessage } from "@/components/common/ErrorMessage";
export default function EditPage({ params }: { params: Promise<{id:string}> }) { const { id } = use(params); const { document, loading, error } = useDocument(id); if (loading) return <LoadingSpinner/>; if (error || !document) return <ErrorMessage message={error || "Không tìm thấy tài liệu."}/>; return <div className="mx-auto max-w-4xl"><h1 className="text-3xl font-bold">Chỉnh sửa tài liệu</h1><p className="mt-2 text-sm text-stone-500">{document.document_id}</p><DocumentForm document={document}/></div>; }
