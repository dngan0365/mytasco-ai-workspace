import { forwardRef, type InputHTMLAttributes } from "react"; import { cn } from "@/utils/cn";
interface Props extends InputHTMLAttributes<HTMLInputElement> { label?: string; error?: string }
export const Input = forwardRef<HTMLInputElement, Props>(({ label, error, className, ...props }, ref) => <label className="block space-y-1.5 text-sm font-medium text-stone-700">{label && <span>{label}</span>}<input ref={ref} className={cn("h-11 w-full rounded-xl border border-stone-200 bg-white px-3.5 text-stone-900 outline-none transition placeholder:text-stone-400 focus:border-emerald-700 focus:ring-2 focus:ring-emerald-700/10", error && "border-red-400", className)} {...props}/>{error && <span className="block text-xs text-red-600">{error}</span>}</label>); Input.displayName = "Input";
