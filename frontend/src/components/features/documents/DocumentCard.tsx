import Link from "next/link"; import { ArrowUpRight, FileText, LockKeyhole } from "lucide-react"; import type { Document } from "@/types/document"; import { formatDate, formatNumber } from "@/utils/formatter";
const colors: Record<string, string> = { Public: "bg-emerald-50 text-emerald-700", Internal: "bg-blue-50 text-blue-700", Confidential: "bg-orange-50 text-orange-700", Restricted: "bg-red-50 text-red-700" };
export function DocumentCard({ document }: { document: Document }) { return <Link href={`/documents/${document.document_id}`} className="group flex h-full flex-col rounded-2xl border border-stone-200 bg-white p-5 transition hover:-translate-y-0.5 hover:border-stone-300 hover:shadow-lg hover:shadow-stone-900/5"><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-stone-100 text-[#142b28]"><FileText className="h-5 w-5"/></span><ArrowUpRight className="h-4 w-4 text-stone-300 transition group-hover:text-stone-700"/></div><h3 className="mt-5 line-clamp-2 font-bold leading-6 text-stone-900">{document.title}</h3><p className="mt-2 text-xs text-stone-500">{document.document_id} · {document.department}</p><div className="mt-auto flex items-center justify-between pt-6"><span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-[10px] font-bold ${colors[document.classification] ?? "bg-stone-100 text-stone-600"}`}><LockKeyhole className="h-3 w-3"/>{document.classification}</span><span className="text-[10px] text-stone-400">{formatNumber(document.word_count)} từ · {formatDate(document.last_updated)}</span></div></Link>; }
